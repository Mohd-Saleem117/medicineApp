import 'package:flutter/material.dart';
import '../main.dart';

class Arr {
  var name, des, bf, ts;
  var a, tot;
  var setDate;
  var setTime;
  Arr(
      {required this.name,
      required this.setTime,
      required this.setDate,
      required this.des,
      required this.a,
      required this.bf,
      required this.ts,
      required this.tot});
}
